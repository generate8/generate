/**
 * @typedef {object} ObjWithValue
 * @property {any} value
 */

/**
 * @typedef {object} Gen    A generator object
 * @property {()=>ObjWithValue} next      A function that returns the next value
 * that this generator can generate.
 *
 */

/**
 *just so that this is considered an ES6 module
 */
export const junk = 123;
